import { Fustat } from 'next/font/google';
 
export const fustat = Fustat({ subsets: ['latin'] });

import { EB_Garamond } from 'next/font/google';
export const ebGaramond = EB_Garamond({ subsets: ['latin'], weight: ['400', '700'] });